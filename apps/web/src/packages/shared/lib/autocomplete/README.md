# Autocomplete compatibility entry point

The autocomplete engine lives in `packages/core/autocomplete`. This directory
keeps the web app's former import path while the application migration is in
progress.

New code imports `@september/core/autocomplete`.
