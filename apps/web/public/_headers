# Cross-origin isolation headers for the deployed browser application.
# Vercel and the Cloudflare Worker apply the same policy to the SPA shell.
/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
  Cross-Origin-Resource-Policy: cross-origin
