export interface ColorConfig {
  border: string;
  bg: string;
  borderLg: string;
  bgHover: string;
  bgActive: string;
  text: string;
  textHover: string;
  ringOffset: string;
  textLight: string;
}
